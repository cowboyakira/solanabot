{
  "name": "solsniper-proxy",
  "version": "2.0.0",
  "description": "Proxy serverless para DexScreener API — SOL SNIPER Bot",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
